os:
  editPreset: "nvim"
gui:
  theme:
    selecteLineBgColor:
      - underline
    selectedRangeBgColor:
      - underline
git:
  autoFetch: false
  autoRefresh: false
  diffRenderers:
    - colorArg: always
      command: delta --dark --paging=never
